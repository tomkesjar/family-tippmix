package kt.tippmix.model;

/* Example to link users to jobs to create a linked table */
// with this you can query all jobs a user has applied to
// with this you can query all users who applied for a specific job


//@Entity
//@Table(name="application")
public class ExampleApplication {
//
//    @Id
//    @GeneratedValue(strategy = GenerationType.IDENTITY)
//    private long id;
//
//      // Many applications can belong to one user:
//      // Many applications can be added in the db for one job:
//    @ManyToOne
//    @JoinColumn(name = "user_id", nullable = false)
//    private User user;
//
//    @ManyToOne
//    @JoinColumn(name = "job_id", nullable = false)
//    private Job job;
//
//    public Application() {}
//
//    public Application(User user, Job job) {
//        this.user = user;
//        this.job = job;
//    }
}
